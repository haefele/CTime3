<ui:UiPage x:Class="CTime3.Apps.WPF.Views.Login.LoginPage"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:local="clr-namespace:CTime3.Apps.WPF.Views.Login"
           xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
           
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           mc:Ignorable="d"
           
           d:DataContext="{d:DesignInstance local:LoginPageViewModel, IsDesignTimeCreatable=False}">
    <StackPanel Orientation="Vertical">
        <ui:TextBox PlaceholderText="Username" Text="{Binding Inner.LoginName, UpdateSourceTrigger=PropertyChanged}" />
        <ui:TextBox PlaceholderText="Type something..." Text="{Binding Inner.Password, UpdateSourceTrigger=PropertyChanged}" />
        
        <ui:Button Appearance="Primary" Content="WPF UI button" Icon="Fluent24" Command="{Binding Inner.LoginCommand}" />
    </StackPanel>
</ui:UiPage>
